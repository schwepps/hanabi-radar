# Prettier honors .gitignore by default (skips .next/, coverage/, next-env.d.ts).
# Only list committed files that Prettier should not reformat.
pnpm-lock.yaml

# Local-only workspace dir (gitignored outside the repo .gitignore).
.context

# Vendored spec/agent doc — kept as authored, not Prettier-owned.
CLAUDE.md

# Generated Supabase types — regenerated by `pnpm db:types`, not Prettier-owned.
src/types/database.ts

# Supabase CLI runtime temp files (gitignored via supabase/.gitignore; Prettier
# doesn't read nested .gitignore, so list them here too).
supabase/.temp/
